#pragma once

/*
    #include "Waterchip/Framework.h" uses loose header files and is the
    equivalent of #include "Waterchip.h" (which bundles headers into one file).
*/

#include "Waterchip/Core.h"
